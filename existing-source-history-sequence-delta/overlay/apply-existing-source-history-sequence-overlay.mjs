import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';import {fileURLToPath} from 'node:url';
const here=path.dirname(fileURLToPath(import.meta.url));const manifest=JSON.parse(fs.readFileSync(path.join(here,'manifest.json'),'utf8'));const sourceDir=path.join(here,'src');
const sha=p=>crypto.createHash('sha256').update(fs.readFileSync(p)).digest('hex');const fail=m=>{throw new Error(`HISTORY_SEQUENCE_OVERLAY_REFUSED:${m}`)};
function src(e){const p=path.join(sourceDir,e.source);if(!fs.existsSync(p))fail(`SOURCE_MISSING:${e.source}`);const got=sha(p),want=e.after_sha256||e.sha256;if(got!==want)fail(`SOURCE_SHA_MISMATCH:${e.source}:${got}`);}
function state(runtime,e){const p=path.join(runtime,e.path);if(!fs.existsSync(p))return'ABSENT';const h=sha(p);if(h===e.before_sha256)return'BEFORE';if(h===(e.after_sha256||e.sha256))return'AFTER';return`OTHER:${h}`;}
function proof(already=false){return{version:manifest.version,status:already?'ALREADY_APPLIED':'APPLIED',base_candidate_sha:manifest.base_candidate_sha,production_base:manifest.production_base,changed_files:manifest.existing_files.map(x=>x.path),added_files:manifest.new_files.map(x=>x.path),...manifest.safety};}
export function applyExistingSourceHistorySequenceOverlay(runtimeDir){const runtime=path.resolve(runtimeDir);for(const e of [...manifest.existing_files,...manifest.new_files])src(e);const es=manifest.existing_files.map(e=>({...e,state:state(runtime,e)})),ns=manifest.new_files.map(e=>({...e,state:state(runtime,e)}));if(es.every(x=>x.state==='AFTER')&&ns.every(x=>x.state==='AFTER')){const p=proof(true);fs.writeFileSync(path.join(runtime,'existing-source-history-sequence-overlay-proof.json'),JSON.stringify(p,null,2)+'\n');return p;}if(!es.every(x=>x.state==='BEFORE'))fail(`NON_ATOMIC_EXISTING_STATE:${es.map(x=>`${x.path}=${x.state}`).join(',')}`);if(!ns.every(x=>x.state==='ABSENT'))fail(`NON_ATOMIC_NEW_STATE:${ns.map(x=>`${x.path}=${x.state}`).join(',')}`);const prepared=[],backups=[],created=[];try{for(const e of es){const target=path.join(runtime,e.path),tmp=`${target}.hs.tmp`,backup=`${target}.hs.backup`;fs.copyFileSync(path.join(sourceDir,e.source),tmp);fs.chmodSync(tmp,fs.statSync(target).mode);if(sha(tmp)!==e.after_sha256)fail(`PREPARED_SHA_MISMATCH:${e.path}`);prepared.push({kind:'existing',target,tmp,backup,after:e.after_sha256});}for(const e of ns){const target=path.join(runtime,e.path),tmp=`${target}.hs.tmp`;fs.copyFileSync(path.join(sourceDir,e.source),tmp);if(sha(tmp)!==e.sha256)fail(`PREPARED_SHA_MISMATCH:${e.path}`);prepared.push({kind:'new',target,tmp,after:e.sha256});}for(const x of prepared.filter(x=>x.kind==='existing')){fs.renameSync(x.target,x.backup);backups.push(x);fs.renameSync(x.tmp,x.target);}for(const x of prepared.filter(x=>x.kind==='new')){fs.renameSync(x.tmp,x.target);created.push(x.target);}for(const x of prepared)if(sha(x.target)!==x.after)fail(`POST_INSTALL_SHA_MISMATCH:${path.relative(runtime,x.target)}`);for(const x of backups)fs.rmSync(x.backup,{force:true});const p=proof(false);fs.writeFileSync(path.join(runtime,'existing-source-history-sequence-overlay-proof.json'),JSON.stringify(p,null,2)+'\n');return p;}catch(e){for(const x of prepared)fs.rmSync(x.tmp,{force:true});for(const p of created)fs.rmSync(p,{force:true});for(const x of [...backups].reverse()){fs.rmSync(x.target,{force:true});if(fs.existsSync(x.backup))fs.renameSync(x.backup,x.target);}throw e;}}
if(process.argv[1]&&path.resolve(process.argv[1])===path.resolve(fileURLToPath(import.meta.url)))console.log('HISTORY_SEQUENCE_OVERLAY_APPLIED',JSON.stringify(applyExistingSourceHistorySequenceOverlay(process.argv[2]||'runtime')));
