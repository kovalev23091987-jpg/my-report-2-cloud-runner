import test from 'node:test';import assert from 'node:assert/strict';import {normalizeLiquidationEvidence,selectLiquidationZones} from '../src/liquidation-evidence.mjs';
test('realized and projected remain separate',()=>{const e=normalizeLiquidationEvidence({realized:[{level_price:105,source:'HTX',status:'CLOSED'}],projected:[{level_price:110,source:'P',status:'CLOSED'}]});assert.equal(e.realized[0].confidence,'OBSERVED');assert.equal(e.projected[0].confidence,'PROJECTED');});
test('largest cluster is not guaranteed TP',()=>{const e=normalizeLiquidationEvidence({projected:[{level_price:110,source:'P',status:'CLOSED'},{level_price:120,source:'P',status:'CLOSED'}]});const z=selectLiquidationZones(e,{current_price:100,direction:'LONG'});assert.equal(z.status,'CLOSED');assert.equal(z.guaranteed_target,false);});
