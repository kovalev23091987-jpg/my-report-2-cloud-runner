import test from 'node:test';import assert from 'node:assert/strict';import {digest} from '../src/upstream-proof-utils.mjs';import {deriveCandidateEntryArea} from '../src/entry-area-rule-v2.mjs';
const now=1_790_000_000_000;
function rule(){const x={schema_version:'tz101-entry-area-strategy-rule-v2',status:'CLOSED',approved_for_analytical_entry:true,rule_version:'entry-area-v1',method:'TRIGGER_BAND_BPS',max_distance_bps:100,max_validity_ms:900000,approved_ts:now-1000};return {...x,persistence:{status:'CLOSED',immutable:true,verification_method:'D1_IMMUTABLE_RECEIPT',receipt_id:'r1',committed_ts:now-500,content_digest:digest(x)}};}
test('strategy level rule deterministically creates per-candidate receipt',()=>{const r=deriveCandidateEntryArea({rule:rule(),decision:{decision_id:'d',snapshot_id:'s',contract_code:'ABC-USDT',direction:'LONG'},campaign_proof:{entry_scenario_anchor:{entry_trigger_price:100,invalidation_price:95},entry_window:{valid_until_ts:now+600000}},execution_reference_price:100.5,observed_ts:now});assert.equal(r.status,'CLOSED');assert.equal(r.inside,true);assert.equal(r.rule_version,'entry-area-v1');assert.ok(r.candidate_receipt.content_digest);});
