name: Telegram candidate validation
on:
  push:
    branches: ['telegram-info-fix/*']
permissions:
  contents: read
concurrency:
  group: telegram-candidate-${{ github.ref }}
  cancel-in-progress: false
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      REPORT2_TELEGRAM_SHADOW_DECISION_AUTO: "0"
      REPORT2_V3_TELEGRAM_NETWORK_ENABLED: "0"
      REPORT2_TELEGRAM_OUTPUT_ENABLED: "0"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Verify exact protected checkpoint and candidate hashes
        run: node runner/validate-v2-tz101-candidate.mjs
      - name: Decrypt exact baseline and apply checked ephemeral overlays
        env:
          REPORT2_PAYLOAD_KEY: ${{ secrets.REPORT2_PAYLOAD_KEY }}
        shell: bash
        run: |
          set -Eeuo pipefail
          mkdir runtime
          node runner/payload-crypto.mjs decrypt payload/report2-runtime.enc "$REPORT2_PAYLOAD_KEY" runtime/payload.tar.gz
          tar -xzf runtime/payload.tar.gz -C runtime
          rm runtime/payload.tar.gz
          node runner/apply-telegram-runtime-overlay.mjs runtime runner/telegram-runtime-overlay.json
          node tz101-shadow-overlay/apply-runtime-overlay.mjs runtime
          node tz101-shadow-overlay/apply-runtime-overlay.mjs runtime
          cp runner/apply-telegram-runtime-overlay.mjs runner/runner-main.mjs runner/telegram-info-runtime.mjs runner/telegram-output.mjs runner/telegram-zero-reason.mjs runner/telegram-readonly-diagnostic.mjs runner/report2-d1-adapter.mjs runtime/
          cp -R telegram-repair-tests runtime/telegram-repair-tests
          cp tz101-shadow-overlay/tests/*.mjs runtime/tests/
          test "$(sha256sum runtime/src/worker.js | awk '{print $1}')" = "1bf1012db1e1f7913d8dd132f7ad95bab7e2c914f2f5f9ece2eb313811a1dd12"
          test "$(sha256sum runtime/src/v3-early-sidecar.mjs | awk '{print $1}')" = "50a4d790428c1322b455d9f393dfa0f8139d98a3527632351d76156ea4afd676"
          test "$(sha256sum runtime/src/v3-telegram-lifecycle-sidecar.mjs | awk '{print $1}')" = "c2cd8e06a4734da2ca8377e8b8591caeee97446daa4d75c2ee77f6c9a2202a33"
      - name: Complete Telegram and TZ 10.1 regression suites - no writes or messages
        working-directory: runtime
        shell: bash
        run: |
          set -Eeuo pipefail
          node --check runner-main.mjs
          node --test --test-reporter=tap telegram-repair-tests/*.test.mjs > telegram-candidate-tests.tap
          node telegram-repair-tests/final-chain-regression.mjs > telegram-final-chain.txt
          node --test --test-reporter=tap \
            tests/tz101-publication-input-runtime.test.mjs \
            tests/tz101-liquidation-context.test.mjs \
            tests/tz101-publication-runtime.test.mjs \
            tests/tz101-publication-wiring-static.test.mjs \
            tests/tz101-scenario-plan.test.mjs \
            tests/tz101-cost-assessment.test.mjs > tz101-candidate-tests.tap
          echo TELEGRAM_V2_TZ101_CANDIDATE_TESTS_PASS
      - name: Bounded read-only production diagnosis
        env:
          REPORT2_D1_BRIDGE_URL: ${{ secrets.REPORT2_D1_BRIDGE_URL }}
          REPORT2_D1_BRIDGE_TOKEN: ${{ secrets.REPORT2_D1_BRIDGE_TOKEN }}
        run: node runner/telegram-readonly-diagnostic.mjs
      - name: Save proof
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: telegram-candidate-proof
          path: |
            runtime/telegram-candidate-tests.tap
            runtime/telegram-final-chain.txt
            runtime/tz101-candidate-tests.tap
            telegram-readonly-diagnosis.json
          retention-days: 14
